pipeline {
    agent any 
    stages {
        stage("build-stage") {
            steps {
                echo "this comes from jenkins pipline : pipline-1 stage : stage-1 step : step-1"
            }
        }
        stage("Test-stage") {
            steps {
                echo "this comes from jenkins pipline : pipline-1 stage : stage-1 step : step-1"
            }
        }
    }
    
}
